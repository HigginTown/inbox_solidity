# Getting started with Ethereum contracts, dApps, and Solidity

This application runs locally with the help of `ganache`. 

Simple contract stores a `message` in an `inbox`. Has `message` and `setMessage` methods. 